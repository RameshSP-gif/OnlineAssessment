# Example environment variables for deployment
# Set a strong JWT secret for production
JWT_SECRET=super-secure-random-string

# Optional: path to SQLite DB file. On Vercel use /tmp/data.sqlite
# SQLITE_DB=/tmp/data.sqlite

# Optional: upload directory for video files. On Vercel use /tmp/uploads
# UPLOAD_DIR=/tmp/uploads

# Optional: S3 / R2 configuration (use for persistent uploads)
# S3_BUCKET=my-bucket
# AWS_ACCESS_KEY_ID=yourkey
# AWS_SECRET_ACCESS_KEY=yoursecret
# AWS_REGION=us-east-1
# S3_ENDPOINT=https://<account>.r2.cloudflarestorage.com   # optional for R2
